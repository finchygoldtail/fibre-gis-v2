import React from "react";

type MapTopBarControllerProps = {
  children: React.ReactNode;
};

export default function MapTopBarController({ children }: MapTopBarControllerProps) {
  return <>{children}</>;
}
